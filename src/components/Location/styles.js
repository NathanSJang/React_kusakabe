import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  locationAndHour: {
    display: 'flex',
    justifyContent: 'space-evenly',
    marginBottom: '13vmin',
  }
}));