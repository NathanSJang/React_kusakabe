import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  backToHomeBtn: {
    display: 'flex',
    justifyContent: 'center',
    marginBottom: '5vmin',
    marginTop: '10vmin',
  },
  comfirmText: {
    textAlign: 'center',
    marginBottom: '2vmin',
  }
}));