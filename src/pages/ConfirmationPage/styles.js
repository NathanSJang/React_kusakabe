import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    height: '100vmin',
  },
  backToHomeBtn: {
    display: 'flex',
    justifyContent: 'center',
    marginBottom: '5vmin',
    marginTop: '10vmin',
  }
}));